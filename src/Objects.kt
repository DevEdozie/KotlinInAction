import java.io.File
/*
object Payroll {
    val allEmployees = arrayListOf<rsk.Person>()

    fun calculateSalary() {
        for ((index, person) in allEmployees.withIndex()) {
            println(person.name)
        }
    }
}

 object Animal{
     var name:String? = null
     var age:Int? = null
     var gender:String? = null
     var type:String? = null

     fun printDetails(){
         println("Name:${name}\nAge:${age}\nGender:${gender}\nType:${type}")
     }
 }

object CaseInsensitiveFileComparator : Comparator<File> {
    override fun compare(file1: File, file2: File): Int {
        return file1.path.compareTo(file2.path, ignoreCase = true)
    }
}

data class rsk.Person(val name: String) {
    object NameComparator : Comparator<rsk.Person> {
        override fun compare(p1: rsk.Person, p2: rsk.Person): Int =
            p1.name.compareTo(p2.name)


    }
}

class A{
    companion object {
        fun bar(){
            println("Companion object called")
        }
    }
}

fun main(args: Array<String>) {
    A.bar()

*/

    /*
    Payroll.allEmployees.add(rsk.Person("Michael"))
    Payroll.allEmployees.add(rsk.Person("Judith"))
    Payroll.calculateSalary()
    CaseInsensitiveFileComparator.compare(File("rsk.User"), File("user"))
    val files = listOf(File("Z"), File("a"))
    println(files.sortedWith(CaseInsensitiveFileComparator))
    val persons = listOf(rsk.Person("Bob"),rsk.Person("Alice"))
    println(persons.sortedWith(rsk.Person.NameComparator))
    */
    /*
    var name ="Bruno"
    var age = 20
    var gender = "Male"
    var type = "Rot"
    Animal.name = name
    Animal.age = age
    Animal.gender = gender
    Animal.type = type
    Animal.printDetails()

}

   */